# Python build/test toolchain for RillML.
#
# Used by:
#   - .github/workflows/pipeline.yml (python-build CI job)
#   - .github/workflows/pipeline.yml (publish-python release job)
#
# Pinned to a compatible minor release (``~=X.Y`` allows patch upgrades
# only) so security fixes land without a manual bump, while breaking
# minor upgrades are still excluded. ``pip install`` resolves the latest
# matching patch at install time; for fully reproducible builds, pin the
# resolved versions via a lockfile (e.g. pip-tools or uv.lock) instead.
# Bump deliberately and re-run CI before trusting a new minor version
# with the release pipeline.

maturin~=1.14
pytest~=8.4
